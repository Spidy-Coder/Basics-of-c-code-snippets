/*Suppose in a code the 
value pi(3.14) is used many times,
 instead of always writing 3.14 everywhere in the code,
  what we can do is define this value
   to some variable, say pi at the beginning
    of the program, and now every time we need the 
    value 3.14, we just need to write pi.

  */

